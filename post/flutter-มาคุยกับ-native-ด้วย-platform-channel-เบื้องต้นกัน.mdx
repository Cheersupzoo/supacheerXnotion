---
title: Flutter | มาคุยกับ Native ด้วย Platform Channel เบื้องต้นกัน
titleEN: Basic Communication with Native By Using Platform Channel
description: >-
  Flutter สามารถสร้างหน้าตา และหลักการทำงาน ของแอปเราได้แทบไร้ขีดจำกัด
  แต่ถึงจุดนึงก็มีบางครั้ง...
descriptionEN: >-
  Flutter provide almost infinite possibility to create an app with
  beatiful UI and great feature. However, there will be some point that
  may required to use native mobile feature...
coverImage: "/img/1__dcN5kyhiuaGCKVZ__gKdDCg.jpeg"
date: "2020-07-05T13:55:02.711Z"
categories: ["Flutter"]
keywords: []
slug: >-
  flutter-มาคุยกับ-native-ด้วย-platform-channel-เบื้องต้นกัน
type: "blog"
---

![](/img/1__dcN5kyhiuaGCKVZ__gKdDCg.jpeg)

<BigLetter>F</BigLetter>{" "}

lutter สามารถสร้างหน้าตา และหลักการทำงาน ของแอปเราได้แทบไร้ขีดจำกัด แต่ถึงจุดนึงก็มีบางครั้งที่เราจำเป็นต้องทำมากกว่านั้น โดยเฉพาะการดึง Native feature มาใช้ อย่างเช่น biometric authentication สแกนลายนิ้วมือ ซึ่งโดยทั่วไปเราสามารถใช้ packages จาก [pub.dev](https://pub.dev/) แล้วเรียกใช้งานใน dart ได้เลย เหมือนมีเวทมนตร์ไม่มีผิด

แล้วยิ่งตอนนี้ มีกลุ่มคนมากมายมาช่วยกันสร้าง packages ให้เราใช้ ซึ่งครอบคลุมการใช้งานส่วนใหญ่ แต่อย่างไรก็ตาม ก็คงมีบางครั้งที่สิ่งที่เราอยากได้จาก Native แต่ยังไม่มีใครทำไว้ เลยถือโอกาสนี้ มาทำความเข้าใจ ว่ามันทำงานกันอย่างไร และลองสร้าง Platform channel แบบง่ายๆกัน

---

#### มาทำความเข้าใจ **Platform Channel** กันก่อน

> ในบทความนี้ เราจะเจาะจง Native เฉพาะ Android Kotlin กับ iOS Swift ซึ่งเป็น mobile platform ส่วน web กับ desktop ไว้รอมันเข้า stable ก่อนละกัน

เราจะเห็นว่า Flutter สื่อสารกับ host ผ่าน channel ตามรูปด้านล่าง

![](/img/0__GE8LneJWuSCz4Iy7.png)

ตัวข้อความที่ส่งไปมา รองรับการใช้ ประเภทตัวแปรพื้นฐานทั้งหมด เช่น bool int String List Map รายละเอียดเพิ่มเติ่ม และหนึ่งในตัวอย่าง MethodChannel สามารถดูได้จาก [docs](https://flutter.dev/docs/development/platform-integration/platform-channels) ต้นทางเลย

ในบทความนี้ เราจะอธิบายภาพรวมมากกว่านี้

Flutter สามารถสื่อสารผ่าน channel ได้ 2 ประเภท

1.  **Method Channel**

ใช้เมื่อเราต้องการเรียกฟังก์ชั่นเป็นครั้งๆไป เรียกแต่ละครั้ง เราจะได้รับ `Future<dynamic>` กลับมา ตัว channel มีลักษณะเป็น bi-directional คือ

> Flutter {'<—>'} Native Host

เราสามารถ invoke หรือเรียกจากฝั่งไหนก็ได้

2. **Event Channel**

ใช้เมื่อเราต้องการรับค่าจากฟังก์ชั่นอย่างต่อเนื่อง เมื่อเรารับมา จะได้รับ `Stream<dynamic>` กลับมา ตัว channel มีลักษณะเป็น one-way

> Flutter {'< — '} Native Host

ตัว Flutter จะฟังข้อความจาก Native ได้อย่างเดียว

---

เพื่อให้เห็นภาพมากขึ้น เรามาลองลงมือเขียนโค๊ดกันหน่อย

เราจะแบ่งเป็นส่วนของ Method Channel กับ Event Channel

---

#### ผลลัพธ์สุดท้ายจะออกมาประมาณนี้

![](/img/1__12LS7akGjnGWcg7BwocnWA.gif)

ใน **Method Channel** เราจะส่งข้อความไปที่ Native แล้วให้ Native ส่งข้อความที่เติมคำลงท้ายกลับมา

ส่วน **Event Channel** จะเราฟัง timer จากฝั่ง Native ซึ่งเราจะเริ่มหรือหยุดฟังก็ได้

> **หมายเหตุ** จะพยายามทำให้โครงสร้างโค๊ดของฝั่ง Kotlin กับ Swift ใกล้เคียงกันที่สุด แต่ถ้ามีพื้นฐานของทั้ง 2 ภาษา จะช่วยได้มาก

---

### เริ่มได้ 🔰

#### มาเตรียมตัวกันก่อน

หลังจากสร้างโปรเจค Flutter ใหม่ โครงสร้างไฟล์ที่เราจะเข้าไปแก้ไขมีตาม

<Gist id="e07eacfaf785dfb14fb916319a747094" />

ไฟล์ที่เราสร้างเพิ่มจะมีเพียง `lib/platform_channel.dart` ส่วนไฟล์ที่เหลือจะมีอยู่แล้ว

---

#### เริ่มทำ **Method Channel** กันก่อน

**มาดูส่วนของ Flutter ก่อน**

เขียน ฟังก์ชั่นมาใช้งาน Method Channel ที่ `lib/platform_channel.dart`

<Gist id="1d71d7eb5d0639d200ea36dba729c125" />

- ประกาศตัวแปร `_methodChannel` เป็นช่องทางสื่อสารแบบ Method Channel
- ฟังก์ชั่น `getStringReturnFromPlatform(String text)` รับข้อความมาแล้ว invokeMethod ส่งไป Native ในแบบตัวแปร Map `{'message': text}` แล้วรับคืนกลับมาเป็น String

หมดแล้วฝั่ง Flutter

**ไปรับมือกับข้อความ บน Kotlin กันต่อ**

ที่ไฟล์ `android/app/src/main/kotlin/com/example/flutter_simple_platform_channel/MainActivity.kt` หน้าตาเดิมๆจะเป็นแบบนี้

<Gist id="ddc6138d8ead37c5210da9320d15c637" />

แก้เป็นตามนี้

<Gist id="391136ac80fae7c871fad553f637d651" />

ทุกครั้งที่เรารันแอป class `MainActivity` ใน Kotlin จะถูกรัน เราเลย override `fun configureFlutterEngine(@NonNull flutterEngine: FlutterEngine)` แล้วตั้งค่า `MethodChannel(flutterEngine.dartExecutor.binaryMessenger, METHODCHANNEL)` แล้วเรียก `.setMethodCallHandler` ซึ่งในนั้น จะเป็นที่ๆเรารับมือกับ invokeMethod ซึ่งเรามีอยู่อันเดียว ชื่อว่า `“sendtext”` ถ้ากลับไปดูใน Flutter ก็จะเป็นชื่อเดียวกันเลย

**ไปดูฝั่ง Swift บ้าง**

ที่ไฟล์ `ios/Runner/AppDelegate.swift` หน้าตาเดิมๆจะเป็นแบบนี้

<Gist id="cb81a7168a71b693040320d00e6e8e2f" />

สิ่งที่เราจะแทรกจะมี 2 ส่วน เหมือนบน kotlin คือ `MethodChannel` กับ `func getStringReturnToDart(text: String)` สุดท้ายได้ออกมาตามนี้

<Gist id="793ff5f157116f51e575ef76e683abfa" />

หลักการก็จะคล้ายกับ Kotlin แต่รอบนี้ชื่อ class จะเป็น AppDelegate แทน และเวลาส่งข้อความกลับใน Kotlin จะเรียก result.success(msg) แต่ใน Swift จะเป็น result(msg)

#### มาส่วนแสดงผลกันบ้าง

กลับไปแก้ที่ `lib/main.dart` กัน

<Gist id="c55b06fd9e388b6da339bffd61431798" />

เมื่อรันแล้วจะได้ผลลัพธ์ ตามนี้

![](/img/1__lGfyIrPKzEWosMN5bAYgEg.png)

---

#### มาต่อกับ Event Channel

<Gist id="a7788bcb971f4480839cf9e83e8d8ddf" />

**เริ่มที่ Flutter เหมือนเดิม**

มาเติม Event Channel ที่ไฟล์ `lib/platform_channel.dart`

เพิ่มอีก 2 ส่วน

- `_eventChannel` หรือช่องทางไว้ฟัง Stream
- `getTimerStream` ฟังก์ชั่นรับ Stream แต่เนื่องจากข้อความที่ส่งผ่าน Method/Event Channel ไม่ได้บ่งบอกประเภทตัวแปร (ถึงแม้เราเองจะรู้) จึงต้อง `.map((dynamic event) => event as int)` เพื่อทำให้ dart รู้ประเภทตัวแปร

เราจะกลับมากับวิธีนำไปใช้ต่อหลังไปเขียนส่วน Native

**ไป Kotlin กันต่อ**

เนื่องจากเราใช้ ReactiveX ในการทำ Timer เลยต้องไปเพิ่มใน dependency กันก่อน

มาเพิ่มที่ `android/app/build.gradle`

<Gist id="37f68cba4f1be6409a8cd62b487c4bfa" />

แล้วแก้ที่ไฟล์ `MainActivity.kt` เหมือนเดิม

<Gist id="d41de8f8b145073970514bbea725463b" />

เช่นเดิม ตั้งค่า `EventChannel(flutterEngine.dartExecutor.binaryMessenger, EVENTCHANNEL)` แล้วเรียก `.setStreamHandler` โดยส่ง object หรือเราจะสร้าง class ที่ extends `EventChannel.StreamHandler` ในนั้นจะมีฟังก์ชั่น onListen กับ onCancel ไว้ถูกเรียกเมื่อมีผู้ฟัง หรือยกเลิกการฟัง ดังนั้นใน onListen เราจึงใช้ ReactiveX สร้าง timer ส่ง events ทุกๆ 1 วินาที

**มาฝั่ง Swift บ้าง**

เพิ่ม dependency ที่ Xcode

![](/img/1__TzDr__vkjSGl3JjHWsqjk3g.png)

รายละเอียดเพิ่มเติม ลองอ่านจาก [https://github.com/ReactiveX/RxSwift](https://github.com/ReactiveX/RxSwift) ได้

มาดูส่วนโค๊ดต่อที่ `AppDelegate.swift`

<Gist id="16e3815179e5b9bd19ed63d58683150a" />

หลักการตรงนี้เหมือนกับฝั่ง Kotlin เลย ต่างกันแค่ส่วน สร้าง class `TimerStreamHandler` ก่อน ถึงนำไปประกาศที่ตัวแปร `handler` แล้วจึงส่งให้ `.setStreamHandler(handler)`

#### พร้อมแล้วนำมาใช้แสดงผลกัน

กลับมาแก้ที่ `main.dart`

<Gist id="88677fc2fcfc031eed3b5fbd0c4435aa" />

แล้วเราก็จะได้หน้าตาสุดท้ายออกมา

![](/img/1__OGgH8XoVHaZaXLB7prDW9w.png)

---

ในที่สุด Method Channel กับ Event Channel ของเราก็เสร็จเรียบร้อยแล้ว ถึงแม้เราจะไม่ได้เรียกใช้อะไรพิเศษจาก Native แต่อย่างน้อยก็เริ่มคุ้นเคยการวิธีการทำงานของ Method Channel กับ Event Channel แล้ว ที่เหลือถ้าใช้ Kotlin/Swift ได้ ก็นำไปต่อยอดได้แล้ว

---

#### แถมอีกหน่อย

มาถึงจุดนี้แล้ว อาจจะสังเกตได้ว่า Method Channel เราเริ่มเรียกที่ฝั่ง Flutter อย่างเดียว ถ้าอยากเรียกจากฝั่ง Native บ้าง ก็จะมีการเซ็ตอัพ คล้ายๆกัน แต่ทำที่คนละฝั่ง

**ฝั่ง Flutter**

```dart
_methodChannel.setMethodCallHandler((call) => 'เช็ค call.method แล้วทำเหมือนใน Native');
```

**ฝั่ง Native**

```dart
methodChannel.invokeMethod(method, ข้อความ)
```

จบแล้วอีกหนึ่งบทความ ถ้าใครตามไม่ทัน หรืออยากลองรันเลย ก็ clone Source code ได้ตามนี้

<GithubBlock
  title="Cheersupzoo/flutter_simple_platform_channel"
  description="A simple Flutter project implement platform channel. This can be use as a template for MethodChannel on forward and…"
  href="https://github.com/Cheersupzoo/flutter_simple_platform_channel"
/>
